[Unit]
Description=Celery beat scheduler
After=network.target

[Service]
User={{ ansible_user }}
Group={{ ansible_user }}
WorkingDirectory={{ project_path }}
ExecStart={{ virtualenv_path }}/bin/celery -A snpProject beat -l info

[Install]
WantedBy=multi-user.target